<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ctnapps.suvelatakipisi">

    <!-- POST_NOTIFICATIONS İzni -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- SCHEDULE_EXACT_ALARM ve USE_EXACT_ALARM İzinleri -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SettingsActivity" />
        <activity android:name=".ProfileSettingsActivity" />
        <activity android:name=".NotificationSettingsActivity" />
        <activity android:name=".ThemeSettingsActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".StatisticsActivity" />

        <!-- BroadcastReceiver'ları ekleyin -->
        <receiver android:name=".NotificationReceiver" android:exported="true"/>
        <receiver android:name=".MedicationBroadcastReceiver" android:exported="true"/>

    </application>
</manifest>
